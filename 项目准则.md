# EvalFrog 项目准则

本文件是 EvalFrog 的持续工作准则。每轮对话开始处理仓库任务前，必须先完整读取本文件；根目录 `AGENTS.md` 负责向后续开发者和 Agent 强制声明该要求。

## 1. 目标与优先级

EvalFrog 第一阶段追求的是“核心闭环最小、架构边界清晰”，不是功能全面。

决策优先级如下：

1. 已冻结的架构不变量与领域边界；
2. `docs/architecture/decisions/` 中的架构决策；
3. `docs/plans/项目实施计划与验收标准.md` 中的阶段范围和门槛；
4. 当前模块的实现与局部优化。

不得为了短期开发速度破坏 IR/DSL 职责、不可变版本、运行状态所有权、Node/Edge 标识、Scheduler/Worker 解耦、数据库/Redis/Kafka 职责等底层边界。

## 2. 每轮工作开始前

1. 完整读取本文件；
2. 读取当前模块对应的实施计划、架构决策和已有学习记录；
3. 检查 Git 分支、工作区状态和远端状态，识别并保留用户的无关改动；
4. 明确本轮范围、验收项和不会实施的内容；
5. 若讨论形成冻结决策，最小化更新已有对应文档，不为同一模块反复新建文档。

## 3. 模块完成与验收

模块只有同时满足以下条件，才可标记完成：

- 计划中的交付物全部存在，且没有用占位实现冒充完成；
- 该模块指定的 Unit、Golden、Contract、Integration、E2E、故障或性能测试按范围通过；
- 关键不变量、失败路径、幂等性、并发和恢复行为有可重复的测试证据；
- 新增或变化的架构决策已经回写原有决策文档；
- 完成“学习文档价值评估”，并按第 5 节处理；
- 工作区不包含准备一并提交的无关修改；
- 验收结果已经向用户报告；若某一门槛需要人工确认，则必须等待确认。

禁止通过跳过测试、关闭 Fencing、弱化事务原子性、删除失败用例或降低断言来获得“通过”。

## 4. Git 与 GitHub 规则

- GitHub 目标仓库名为 `evalfrog`；首次创建默认使用 private，公开必须由用户确认；
- 初始架构、计划和项目治理文档可以形成一个独立的文档基线提交；
- 模块代码未通过该模块验收前不得提交为“完成”；通过验收后必须形成该模块独立、可识别的 commit；
- 不把多个已验收模块压成一个含义模糊的提交，也不把无关改动混入模块提交；
- 提交信息使用清晰的 Conventional Commit，例如 `feat(runtime): complete M4 engine state machine`；
- 远端可用时，验收后的提交应推送到 GitHub；未成功推送时必须明确报告，不得把本地 commit 描述为已发布；
- 不执行 force-push，不擅自 amend 已向用户报告的提交，不覆盖远端历史；
- 创建分支时使用 `codex/` 前缀；是否采用 PR 由当时协作流程决定，不能为了开 PR 提前提交未验收代码。

每个模块的提交前清单：

```text
[ ] 模块范围与交付物完成
[ ] 计划要求的测试全部通过
[ ] 失败路径、并发与恢复场景已验证
[ ] 架构决策/契约文档已同步
[ ] 学习文档价值已判断并记录
[ ] 无无关文件进入暂存区
[ ] 验收结果已报告，必要的人工确认已获得
[ ] commit 信息能够独立说明该模块
```

## 5. 学习文档规则

`docs/learning/学习文档.md` 用于一周后的面试复习，不是开发流水账。每完成一个模块，都必须判断该模块是否值得写入学习文档：

- 值得：在模块 commit 前更新学习文档；
- 暂不值得：仍需在模块验收记录中写明“不写入”的理由；
- 判断标准：是否体现架构边界、重要权衡、可靠性机制、数据一致性、性能扩展、安全隔离，或能形成高频面试问题；
- 学习文档只能描述已冻结或已实现并验证的事实；规划中的能力必须明确标为“待实现”，不得包装成项目成果。

优先沉淀以下内容：

1. 该模块解决什么问题，为什么需要独立边界；
2. 最终方案、备选方案及取舍理由；
3. PostgreSQL、Redis、Kafka 分别承担什么，为什么不能互相替代；
4. 一致性、幂等、并发、重试、超时、恢复和可观测性设计；
5. 容量假设、性能瓶颈、水平扩展方式和失效场景；
6. 对应代码、Schema、消息契约和测试用例的位置；
7. 高频面试问答，以及 1 分钟和 5 分钟两种讲解版本；
8. 容易说错或被追问的边界条件。

## 6. 文档与实现一致性

- README 只陈述当前真实状态；规划能力必须明确标注；
- 同一主题优先更新已有文档，避免重复、冲突和文档碎片化；
- Contract、Migration、消息 Schema 和状态机变化必须与实现同一模块验收；
- 图、示例和命令必须可读、可执行或明确标注为目标设计；
- 模块提交前检查 README、架构决策、实施计划和学习文档之间没有矛盾。

## 7. 当前阶段事实

- 当前已完成并确认的是架构基线、总体结构图、M0-M12 实施计划，以及 M0-M7：仓库护栏、IR/Catalog、Compiler/DSL/Source Map、Definition 生命周期、Runtime Domain/Engine、Runtime PostgreSQL 与 Outbox/Inbox、Project 公平 Scheduler/Scheduling Redis、Kafka 分布式执行骨架、Worker Runtime/API、Execution Context Cache-Aside 与 Attempt Recovery；
- M0 仅证明四个进程空壳、配置、基础设施、技术原语和依赖边界可运行，不能声称系统已具备 Workflow 创建或执行能力；
- M1 只证明 Human Web 与 Agent CLI 共用的 IR Schema、Parser、Validator、Catalog、Canonical JSON/Hash 和 Diagnostic 契约可用；
- M2 只证明确定性 IR→DSL/Source Map 编译、Control Graph 静态分析、Runtime DSL 自校验和 Operation Compatibility 契约可用；
- M3 证明 Project 权限与资源双重授权、不可变 Draft/Version/Snapshot、SaveDraft 乐观锁、Publish 幂等与事务原子性、Copy/Rollback 语义和第一批 Definition API 可用；M3 的 Test 只编译 Snapshot，尚未创建真实 Run；
- M4 证明 Run/Node Run/Attempt 封闭状态机、不可变 Snapshot Binding、Branch/OR-Join/Skipped、Effective Output、Retry/Recovery、Fail-Fast、Termination Intent 和确定性 Engine Harness 可用；M4 不包含数据库持久化或真实 Worker；
- M5 证明 TestDraft/Production CreateRun 来源约束、Pending Run + RunCreated Outbox、Engine Inbox/CAS、完整图原子初始化、Attempt Lease/Fencing、两阶段 Output 接受、Outbox Relay 恢复和确定索引路径可用；M5 只实现 Runtime Event DTO 与 Publisher Port，没有接入真实 Kafka Broker；
- M6 证明以 Project 为唯一公平单元的等权 Max-Min、空闲借用、固定 Lane、Redis Credit/Reservation/Lease/Fencing、Global/Pool Dispatch Window、Redis 丢失后数据库重建，以及 `ready → queued + Attempt + Node Task Outbox` 原子派发可用；
- M7 证明 Task/Runtime Event 版本化 Kafka Contract、真实 Broker 至少一次投递、Claim 后 ACK、通用 Worker Runtime、本地 Slot 背压、Worker API、能力同构注册、Execution Context Cache-Aside、Cache 故障回源、Lease Lost/Recovery 与 Fencing 可用；M7 仅使用 local/test 协议测试 Executor，不包含真实 HTTP/RPC 或 Python Sandbox；
- 下一开发阶段从 M8“HTTP/RPC 节点与 Managed Resource Runtime”开始；
- 每个后续模块仍必须独立完成测试、学习价值评估、commit 和远端 CI 核验。
