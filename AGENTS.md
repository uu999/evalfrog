# EvalFrog 工作区指令

在本仓库执行任何分析、设计、编码、测试、文档或 Git 操作之前，必须完整读取根目录的 `项目准则.md`，并把它作为本轮工作的强制约束。

随后只读取与当前模块直接相关的架构决策和实施计划，避免用聊天中的临时结论覆盖已落盘基线。

如果 `项目准则.md` 与更高优先级指令冲突，遵循更高优先级指令，并在交付说明中指出冲突；否则不得跳过准则中的模块验收、学习文档评估和提交规则。
